[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# SPA routing: serve index.html for all routes so refresh/deep-links work.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
